# Helper Utilities 🛠️

A collection of reusable TypeScript/JavaScript utility modules for common backend tasks.

## 📦 Write Once, Use Anywhere

These helper utilities are designed to be framework-agnostic and easily portable across different projects. Simply copy the files you need and integrate them into your codebase.

```

## 🚀 Installation

1. Copy the desired utility files (`.ts` or `.js`) to your project
2. Install peer dependencies if needed:
   - For `validateRequest`: `npm install joi @types/joi express`
3. Import and use in your code

## 📝 TypeScript Support

Both TypeScript (`.ts`) and JavaScript (`.js`) versions are provided. Use the TypeScript versions for full type safety and IDE autocomplete support.

## 🤝 Contributing

Feel free to extend or modify these utilities to fit your specific needs. These are designed as starting points for common patterns.

## 📄 License

Open source - use freely in your projects.
```
